# Nuxt dev/build outputs
.output
.data
.nuxt
.nitro
.cache
.wrangler
dist
bridge-cli/bin

# Node dependencies
node_modules
.pnpm-store

# Logs
logs
*.log

# Misc
.DS_Store
.fleet
.idea

# Test coverage
coverage

# Local env files
.env
.env.*
!.env.example
# Nothing reads .dev.vars since #412 — .env is the one local configuration file.
# Still ignored because a checkout older than that ticket may hold one full of
# real secrets, and un-ignoring it puts those one `git add -A` away from a commit.
.dev.vars
.dev.vars.*

# Parallel-round worktrees (complete checkouts inside the repo)
.claude/worktrees/
.worktrees/


.claude/worktrees/metagame-min-points/.data
.claude/worktrees/top-cards-screen/.data