diff --git a/dist/module.mjs b/dist/module.mjs
index a1aa9162a974596a5f27207b1c3c2ae1d8caef7e..792c1bba1ccb81d809998390a96483d93a8e0684 100644
--- a/dist/module.mjs
+++ b/dist/module.mjs
@@ -1112,15 +1112,10 @@ const module$1 = defineNuxtModule({
     if (!nuxt.options.dev && hub.hosting.includes("cloudflare")) {
       setupCloudflare(nuxt, hub);
     }
-    if (nuxt.options.dev) {
-      const workspaceDir = await findWorkspaceDir(rootDir);
-      const gitignorePath = join(workspaceDir, ".gitignore");
-      const gitignore = await readFile(gitignorePath, "utf-8").catch(() => "");
-      const relativeDir = relative(workspaceDir, hub.dir);
-      if (!gitignore.includes(relativeDir)) {
-        await writeFile(gitignorePath, `${gitignore ? gitignore + "\n" : gitignore}${relativeDir}`, "utf-8");
-      }
-    }
+    // stream-keepr patch: never append hub.dir to a .gitignore. The repo already
+    // ignores `.data`, and from a worktree nested in the repo findWorkspaceDir
+    // resolves to the parent checkout, so this wrote `.claude/worktrees/<name>/.data`
+    // into the main checkout's .gitignore on every dev start.
   }
 });
 
